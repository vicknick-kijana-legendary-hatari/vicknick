class NotificationService {
  initialize() {
    return {
      enabled: false,
      reason: 'Push notifications are prepared for future API integration.'
    };
  }
}

module.exports = { NotificationService };
